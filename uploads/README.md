# uploads

Place any image assets (screenshots, avatars, exported pictures) here.

For images that should ship inside the app and be referenced from XAML
(e.g. `Source="myimage.png"`), put them in `Resources/Images/` instead —
MAUI processes that folder into platform-native image resources.
